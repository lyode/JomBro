<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>JomBro - "bro kang tau Lai liau!"</title>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }
    </style>
</head>
<body>
    <div id="root"></div>
    <script type="text/babel">
        const App = () => {
            const [posts, setPosts] = React.useState([
                {
                    id: 1,
                    user: { name: '不想早起的大罗', avatar: 'https://images.unsplash.com/photo-1494790108755-2616b612b469?w=100&h=100&fit=crop&crop=face' },
                    time: '1 jam lepas',
                    content: 'Wah today cycle around Putrajaya! 🚴‍♀️ Weather cantik gila! #CyclingLife',
                    image: 'https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=600&h=400&fit=crop',
                    likes: 68
                }
            ]);

            return (
                <div className="min-h-screen bg-gray-100">
                    <div className="bg-white shadow-sm border-b sticky top-0">
                        <div className="max-w-6xl mx-auto px-4 py-3">
                            <div className="flex items-center justify-between">
                                <h1 className="text-2xl font-bold text-yellow-600">JomBro</h1>
                                <span className="text-sm text-gray-600 italic">"bro kang tau Lai liau!"</span>
                            </div>
                        </div>
                    </div>
                    
                    <div className="max-w-2xl mx-auto px-4 py-6">
                        <div className="bg-white rounded-xl shadow-sm border p-4 mb-4">
                            <button className="w-full bg-yellow-600 text-white rounded-full px-6 py-3 font-medium">
                                Apa lu fikir? Share lah bro!
                            </button>
                        </div>
                        
                        {posts.map(post => (
                            <div key={post.id} className="bg-white rounded-xl shadow-sm border mb-4 p-4">
                                <div className="flex items-center space-x-3 mb-3">
                                    <img src={post.user.avatar} className="w-12 h-12 rounded-full object-cover" />
                                    <div>
                                        <h3 className="font-bold">{post.user.name}</h3>
                                        <p className="text-sm text-gray-500">{post.time}</p>
                                    </div>
                                </div>
                                <p className="mb-3">{post.content}</p>
                                <img src={post.image} className="w-full h-48 object-cover rounded mb-3" />
                                <div className="flex justify-around pt-2 border-t">
                                    <button className="text-gray-600 hover:text-blue-600">
                                        👍 {post.likes} Steady
                                    </button>
                                    <button className="text-gray-600 hover:text-blue-600">💬 Comment</button>
                                    <button className="text-gray-600 hover:text-blue-600">📤 Kongsi</button>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            );
        };

        ReactDOM.render(<App />, document.getElementById('root'));
    </script>
</body>
</html>
